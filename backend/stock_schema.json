{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "TickerInput",
  "description": "Fields available on each ticker object passed to stock agents.",
  "type": "object",
  "properties": {
    "symbol": {
      "type": "string",
      "description": "Stock symbol"
    }
  },
  "required": ["symbol"]
}
