# This module is deprecated. Market cap is now fetched from market data snapshots
# (fundamentalRatios tick 258) instead of the fundamental data API, which requires
# a paid subscription and is not available for all securities.
