# ── LLM ─────────────────────────────────────────────
LLM_MODEL=gpt-4o
OPENAI_API_KEY=sk-...
ANTHROPIC_API_KEY=sk-ant-...
GOOGLE_API_KEY=...

# ── Agent behavior ───────────────────────────────────
AGENT_MODE=parallel             # parallel | chain
LLM_TEMPERATURE=0.2
LLM_MAX_TOKENS=4096
LLM_TIMEOUT_SECONDS=60
LLM_MAX_RETRIES=3

# ── Concurrency ──────────────────────────────────────
MAX_CONCURRENT_PIPELINES=10
MAX_CONCURRENT_AGENTS=8

# ── Search (Tavily) ──────────────────────────────────
TAVILY_API_KEY=tvly-...
SEARCH_ENABLED=false            # true | false
SEARCH_MODE=tool_call           # prefetch | tool_call
SEARCH_MAX_RESULTS=5            # number of results per search call
SEARCH_DEPTH=basic              # basic | advanced
SEARCH_MAX_TOOL_ROUNDS=10       # max LLM↔tool cycles before forcing final answer

# ── Output ───────────────────────────────────────────
OUTPUT_FORMAT=yaml              # yaml | json
OUTPUT_DIR=./outputs

# ── Database ─────────────────────────────────────────
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=stock_agents
POSTGRES_USER=sa_user
POSTGRES_PASSWORD=changeme

# ── Backend ──────────────────────────────────────────
BACKEND_PORT=8000
AI_SERVICE_HOST=ai-service          # use "localhost" when running outside Docker
AI_SERVICE_PORT=8001
SECRET_KEY=change-this-in-production
CORS_ORIGINS=http://localhost:3000
AI_SERVICE_HOST=localhost

